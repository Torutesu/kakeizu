# 内部仕様書と乖離一覧

**対象:** 戸籍家系図システム
**基準文書:** クライアント向け要件定義書 REQ-001（Google Docs）
**監査日:** 2026-09-06（最終更新 2026-09-06）
**目的:** 要件定義書の各項目について、実装の所在・現況・要件との乖離を洗い出し、改善の起点とする。

クライアント向け文書は「何をできるようにするか」までしか書かない方針のため、
実装の詳細・判断の根拠・既知の欠落は本書に集約する。

---

## 1. 乖離サマリ

優先度は「相続実務での実害の大きさ × 契約上の位置づけ」で付けている。

| ID | 分類 | 内容 | 優先度 |
|---|---|---|:--:|
| G-01 | ~~未実装~~ | ~~本籍が抽出できない~~ → **解消済み**（戸籍を独立要素として追加。転籍の履歴を保持する） | 済 |
| G-02 | ~~欠陥~~ | ~~2モデル照合の「片方だけが抽出した人物」が表示されない~~ → **解消済み**（指摘一覧に表示。E2Eで回帰を防止） | 済 |
| G-03 | ~~欠落~~ | ~~要確認の一覧がない~~ → **解消済み**（`components/IssuesPanel.tsx`） | 済 |
| G-04 | ~~要件超過~~ | ~~Googleログインが有効~~ → **解消済み**（削除。Supabase側のプロバイダ無効化は別途必要） | 済 |
| G-05 | ~~設定~~ | ~~2モデル照合が既定オフ~~ → **解消済み**（キー2社以上で既定オン・同時実行化） | 済 |
| G-06 | 未実装 | **画像の前処理が未着手。** 要件6章「実施する事項」に含めている | 中 |
| G-07 | 未実施 | **モデルの比較検証が未実施。** 仕組みはあるが実データ待ち | 中 |
| G-08 | ~~品質~~ | ~~PDFがA4 1ページ固定~~ → **解消済み**（用紙選択・分割出力・書き出し前の結果表示） | 済 |
| G-09 | ~~要件超過~~ | ~~監査ログ画面が残存~~ → **解消済み**（画面・記録・テーブルをすべて削除） | 済 |
| G-10 | ~~設定~~ | ~~`AI_NO_TRAINING_CONFIRMED` がコメントアウト~~ → **解消済み**（既定で有効化し警告を明記） | 済 |
| G-11 | 未検証 | `gemini-3.1-pro` のモデルIDが解決するか未確認。失敗時は静かに2.5 Proへ落ちる | 中 |
| G-12 | 未検証 | Geminiのプロンプトキャッシュが最小トークン数に届いていない可能性 | 低 |
| G-13 | ~~データ~~ | ~~`public/` に実データが残置~~ → **解消済み**（gitignore済みの `testdata/` へ退避） | 済 |
| G-14 | ~~未記載~~ | ~~招待メールが送信されない~~ → **解消済み**（招待メールを実装） | 済 |
| G-15 | 未記載 | キーボードショートカット10種が要件にない（実装済み） | 低 |
| G-16 | 未記載 | ズーム感度などの表示設定が要件にない（実装済み） | 低 |
| G-17 | 矛盾 | 「スマートフォンでの編集は対象外」と書いたが、狭い画面向けのドロワーを実装済み | 低 |

---

## 2. 機能別の詳細

### 2.1 ログインと利用者の管理（要件4.1）

| 要件 | 実装 | 現況 |
|---|---|---|
| メールアドレスとパスワードでログイン | `app/login/page.tsx` `signInWithPassword` | 実装済み |
| 招待されたメールのみ登録可 | `supabase/migrations/0005_invite_only.sql` `enforce_invite_only()` | 実装済み。`auth.users` の BEFORE INSERT トリガで DB 層で拒否する |
| 利用者の追加・権限変更・削除 | `app/settings/members/page.tsx`, `lib/db/members.ts` | 実装済み |
| 招待の取り消し | `member.revoke_invitation` | 実装済み |

**G-04（解消済み）** Googleログインのボタンとハンドラを削除した。
`app/auth/callback/route.ts` はメール確認リンクの受け口として引き続き使用する（OAuth用ではない）。

**残作業:** Supabase ダッシュボード側で Google プロバイダを無効化すること。
コードから呼ばなくなっただけで、プロバイダ自体が有効なままだと
`/auth/v1/authorize?provider=google` を直接叩けば認証が成立しうる。

**G-14（解消済み）** 招待メールを送信するようにした。

従来は招待の行を作るだけで、招待された人は自分でサインアップする必要があった。
本人に通知が届かず、口頭で伝える運用が前提になっていた。

**実装（`app/api/invitations/route.ts`）:**

処理の順序が重要で、次のとおりでなければならない。

1. **利用者自身のセッション**で `invitations` に行を作る。adminでなければRLSがここで拒否する
2. その後、サービスロールで `auth.admin.inviteUserByEmail` を呼ぶ

順序が逆だと、**管理者以外でも招待メールを送れてしまう**。
またこの順序は `enforce_invite_only` トリガの前提でもある。
`inviteUserByEmail` は `auth.users` に行を作るため、招待の行が先に無いとトリガに弾かれる。

**本人確認について:** 招待メールのリンクを開けた人だけがアカウントを有効にできるため、
メールアドレスの到達確認が本人確認を兼ねる。加えて `enforce_invite_only` トリガにより、
招待されていないアドレスでの登録はDB層で拒否される（二重の防御）。

**サービスロールキーの扱い:** `SUPABASE_SERVICE_ROLE_KEY` はRLSを完全に迂回するため、
`lib/supabase/admin.ts` に `import 'server-only'` を付けてクライアントバンドルへの
混入を防いでいる。用途は招待メールの送信のみに限り、データの読み書きには使わない
（権限検査をRLSに委ねる方針が崩れるため）。ビルド後の `.next/static/` に
`SERVICE_ROLE` の参照がないことを確認済み。

**キー未設定でも招待自体は成立する。** メール送信だけが失敗し、
「このメールアドレスで登録すれば参加できます」と管理者に伝える。
招待の行は残るため、従来どおり口頭で伝える運用に退避できる。

**既存ユーザー宛は失敗ではない。** 招待の行は有効で、その人が次にログインした時点で
メンバーになる（`accept_pending_invitations`）。エラーとして見せると管理者が
何度も招待をやり直すことになるため、`already_registered` として区別している。

**回数制限（対応済み）** 招待は任意のアドレスへメールを送れる経路であり、
管理者アカウントが乗っ取られた場合に迷惑メールの送信元になりうる。2段階でかけている。

| 対象 | 上限 | 実装 |
|---|---|---|
| 利用者ごと | 1時間あたり10件 | `check_rate_limit('invitation', ...)` |
| 事務所ごと | 1日あたり30件 | `invitations` の直近24時間の件数を数える |

事務所単位は招待の行そのものを数えるため、専用のカウンタを持たない
（`created_at` があり、参照はRLSで自組織のadminに限られる）。

**利用者単位は 0006 のレート制限を一般化して流用した（0008）。** 操作の種類を
`action` 列で持つ汎用テーブル `rate_limits` に移行し、`check_analysis_rate_limit` は
汎用関数へ委譲する薄いラッパとして残している（呼び出し側の変更は不要）。

**確認できない場合は通さない（フェイルクローズ）。** マイグレーションの適用漏れ等で
保護が消えた状態を黙って通すと、消えたことに気づけないため 503 を返す。

制限の検査は権限検査（RLS）より前に走るため、管理者以外が試した場合も
自分の枠を消費する。他の利用者への影響はなく、探索的な試行の抑止にもなる。

### 2.2 案件の管理（要件4.2）

| 要件 | 実装 | 現況 |
|---|---|---|
| 案件の作成（案件名・顧客名） | `lib/db/projects.ts` `createProject` | 実装済み |
| 一覧・最終更新日時 | `app/projects/page.tsx` | 実装済み |
| 担当者の割り当て・解除 | `components/ProjectAssignDialog.tsx` | 実装済み |
| 案件の削除（関連データも削除） | `deleteProject` + `on delete cascade` | 実装済み。Storage 上のファイルも削除する |

乖離なし。

### 2.3 戸籍の取り込み（要件4.3）

| 要件 | 実装 | 現況 |
|---|---|---|
| PDF・画像を複数まとめて | `components/KosekiUploadDialog.tsx` | 実装済み。`accept="application/pdf,image/jpeg,image/png,image/webp"`、1ファイル20MB |
| 一覧と状況（未処理・完了・失敗） | `koseki_files.analysis_status` (`pending`/`success`/`failed`) | 実装済み |
| 原本を後から開いて確認 | `components/KosekiFilesPanel.tsx` 署名付きURL | 実装済み。ただし**ダウンロード**であり、画面内プレビューではない |
| 読み取りのやり直し（モデル選択可） | `KosekiFilesPanel.tsx` + `AnalysisOverride` | 実装済み |
| 削除 | 実装済み | |

**注記（要件の解釈）** 「原本を後から開いて確認できること」は、現状ダウンロードして別ウィンドウで開く形。
読み取り結果と原本を並べて確認したい場面が実務では多いはずで、画面内プレビューがあるほうが確認効率は上がる。
要件上は満たしているが、改善候補として記録する。

### 2.4 戸籍の読み取り（要件4.4）

| 要件 | 実装 | 現況 |
|---|---|---|
| 氏名・性別・生年月日・死亡年月日・**本籍**・続柄 | `lib/analysis/schema.ts`, `lib/koseki-prompt.ts` | **本籍のみ未実装（G-01）** |
| 親子・婚姻・養子縁組・離婚の関係 | 同上 `familySchema` | 実装済み |
| 和暦→西暦、元表記の保持 | `original_date` / `date` の2フィールド | 実装済み |
| 読み取れない箇所は空欄、推測しない | プロンプト原則4・6 | 実装済み（プロンプトによる指示） |
| 同一人物のまとめ | `utils/mergeFamilyData.ts` + プロンプトのステップ1 | 実装済み |
| 使用モデルの記録 | `koseki_files.analysis_model` | 実装済み |

#### G-01: 本籍が抽出できない（解消済み）

現在のスキーマが持つ場所の情報は `birth.place`（出生地）と `death.place`（死亡地）のみで、
**本籍（戸籍の所在地）に相当するフィールドが存在しない。** プロンプトにも「本籍」の語が一度も出てこない。

相続実務では本籍は戸籍を追跡する起点であり、転籍の追跡にも必要な情報のため、欠落の影響は大きい。

**対応済み。戸籍（registry）を第一級の要素として追加した。**

人物に本籍フィールドを1つ足す案は採らなかった。本籍は戸籍に属する情報であり、
1人が生涯に複数の戸籍（出生時・婚姻後・転籍後・改製後）に登場する。
人物に1つだけ持たせると、**転籍前の本籍が上書きで失われ、戸籍の追跡ができなくなる。**

追加した構造（`RegistryData`）:

| 項目 | 内容 |
|---|---|
| `registered_domicile` | 本籍の原文表記 |
| `head_of_family` | 筆頭者（戦前は戸主）。本籍と組で戸籍を特定する |
| `registry_type` | `current`（現在戸籍）/ `removed`（除籍）/ `revised`（改製原戸籍） |
| `member_ids` | その戸籍に記載されている人物のid。1人が複数の戸籍に属してよい |

反映箇所:
- `lib/analysis/schema.ts` の `kosekiRegistrySchema`、および `lib/koseki-prompt.ts` の
  Gemini用スキーマ。**両者は必ず一致させること**（片方だけ直すとプロバイダ間で結果が変わる）
- プロンプトの実行プロセスに「ステップ1: 戸籍そのものの把握」を追加し、
  「本籍は人物ではなく戸籍に属する。人物側に本籍を書かないこと」を明示
- `sanitizeFamilyTreeData`: 未知の人物idの参照を除去する。ただし
  **記載人物が0人になっても戸籍自体は残す**（本籍だけでも追跡の手がかりになるため）
- `mergeFamilyTreeData`: 本籍＋筆頭者の組で同一戸籍とみなして統合する。
  転籍で本籍が異なる場合は別件として残す。本籍も筆頭者も不明な戸籍は互いに
  区別できないため名寄せしない。取り込み側のidは名寄せ結果に読み替える
- `components/RegistriesPanel.tsx`: 戸籍を単位として一覧表示し、記載人物から移動できる
- Excel出力: 人物一覧に「本籍」列（複数戸籍は ` / ` 区切り）、加えて「戸籍」シート

**`registries` は解析出力スキーマで必須にした。** 省略可能にすると本籍が丸ごと
落ちても検証を通ってしまうため。保存データ側（`FamilyTreeData.registries`）は
v1形式との互換のため省略可能としている。

### 2.5 家系図の作成と編集（要件4.5）

| 要件 | 実装 | 現況 |
|---|---|---|
| ドラフトの自動作成 | `utils/treeLayout.ts` | 実装済み |
| 世代ごとの整列 | 同上（再帰的サブツリー配置） | 実装済み |
| 婚姻・離婚・養子縁組の区別 | `components/FamilyTreeLines.tsx` | 実装済み。婚姻=二重線、離婚=破線、養子=破線の系統線 |
| 拡大・縮小・移動・全体表示 | `components/FamilyTree.tsx` | 実装済み |
| 人物の追加・修正・削除 | `AddPersonDialog` / `PersonEditDialog` | 実装済み |
| 関係の追加・修正・削除 | `RelationshipEditDialog` | 実装済み |
| 手動配置の保持 | `manualPosition` フラグ | 実装済み |
| 数え年 | `utils/age.ts` | 実装済み |
| 氏名で検索・移動 | `searchPersons` + `focusPerson` | 実装済み |
| 関係者の把握 | `relatedIds` + `RelationEmphasis` | 実装済み |
| 取り消し・やり直し | `hooks/useUndoRedo.ts` | 実装済み |
| 自動保存 | `useFamilyData` 800msデバウンス | 実装済み |
| 同時編集で上書きしない | `lib/db/trees.ts` `expectedVersion` による楽観ロック | 実装済み |

**G-17（矛盾）** 要件7章で「スマートフォンでの編集は対象外」としているが、
`components/FamilyTreeApp.tsx` に `max-lg:` のドロワー実装があり、幅390pxでも編集できる。
実装が要件を上回っている状態。要件から「対象外」を外すか、実装をタブレット幅までに留めるかを決める。
**動くものを対象外と書くと、動かなくなったときに揉める**ため、実態に合わせるのが望ましい。

**G-15 / G-16（未記載）** 実装済みだが要件にない。契約上は問題ないが、
検収時に「これは何か」となるため、要件に1行足しておくほうが親切。

実装済みのキーボードショートカット（`components/ShortcutHelpDialog.tsx`、`?` キーで一覧表示）:

| キー | 動作 | 編集権限 |
|---|---|:--:|
| `Cmd/Ctrl + K` | 人物を検索 | 不要 |
| `F` | 家系図全体を表示 | 不要 |
| `Esc` | 選択を解除・ダイアログを閉じる | 不要 |
| `?` | ショートカット一覧を表示 | 不要 |
| `Cmd/Ctrl + Z` | 元に戻す | 必要 |
| `Cmd/Ctrl + Shift + Z` | やり直し | 必要 |
| `Cmd/Ctrl + S` | 保存 | 必要 |
| `N` | 新しい人物を追加 | 必要 |
| `E` | 選択中の人物を編集 | 必要 |
| `Delete` | 選択中の人物を削除 | 必要 |

編集権限がない利用者には、編集系のショートカットは一覧に表示されない。

このほか `components/SettingsDialog.tsx` にズーム感度などの表示設定がある（G-16）。

### 2.6 読み取り結果の点検（要件4.6）

| 要件 | 実装 | 現況 |
|---|---|---|
| 論理矛盾の自動検出と印 | `utils/consistency.ts` | 実装済み。error 4種・warning 4種 |
| 印を付けた理由の表示 | `PersonNode.tsx` の `<title>` ツールチップ | 実装済み |
| 2モデル照合と食い違いの提示 | `lib/analysis/ensemble.ts` + `components/IssuesPanel.tsx` | 実装済み（G-02解消） |

#### G-02: 照合結果の一部が表示されない（解消済み）

`compareExtractions()` は `cross_person_missing_in_primary`（照合側だけが抽出した人物）を
`personIds: []` で返す。primary 側に対応する人物が存在しないため、付与先の人物がいないのが理由。

`buildUncertaintyMap()` は personIds を辿って印を付ける実装のため、**personIds が空の指摘は誰にも付かず、
画面のどこにも出ない。** 検出して保存されているのに、人の目に触れない状態になっている。

これは**取りこぼし**の指摘であり、2モデル照合で最も価値のある情報の一つ
（片方のモデルだけが見つけた人物＝もう片方が見落とした可能性）。ここが出ないと照合の効果が半減する。

**対応済み。** `components/IssuesPanel.tsx` を追加し、人物に紐づかない指摘も一覧に表示するようにした。
移動先の人物がいない指摘はボタンを無効化し、「家系図に該当する人物がいないため、移動できません」と明示している
（押しても反応しない不具合に見えないようにするため）。

E2E（`e2e/family-tree.spec.ts`）に回帰テストを置いた。検出できていても画面に出ないという不具合は
単体テストでは防げないため、この層でしか守れない。

#### G-03: 要確認の一覧がない（解消済み）

現状、要確認は人物カードの印でしか表現されない。数十人〜百人規模の家系図で、
**印の付いた人物を探すには全体を目で走査するしかない。** 確認箇所を絞るという機能の狙いが達成できていない。

**対応済み。** 右サイドバーの最上部に配置した。

- error（内容の矛盾）と warning（確認をおすすめする箇所）で区分し、件数をバッジ表示
- クリックで該当人物を選択し、キャンバスをその位置へ移動する
- 人物に紐づかない指摘もここに出る（G-02）
- 指摘が0件のときは「指摘がないことは正しさの証明にはなりません」を併記する。
  一覧が空であることを安全の根拠と誤解させないため

**未対応の改善余地:** 確認済みのチェックを付ける機能。
データ構造の追加が必要で、どの単位（指摘ごと／人物ごと）で持つかの設計判断が要る。

### 2.7 成果物の出力（要件4.7）

| 要件 | 実装 | 現況 |
|---|---|---|
| PDF出力 | `utils/exportPdf.ts` + `utils/pdfLayout.ts` + `components/PdfExportDialog.tsx` | 実装済み（G-08解消） |
| Excel出力 | `utils/exportExcel.ts` | 実装済み。人物一覧13列＋家族関係5列 |
| データの保存・読込（置換/統合の選択） | `useFamilyData.importFamilyTreeData` | 実装済み |

**G-08: PDFがA4 1ページ固定（解消済み）**

家系図全体をA4 1ページに収まるよう縮小していたため、人物が増えるほど縮小率が上がり、
大規模な家系図では文字が読めなくなっていた。

**対応済み。** 書き出し前に設定を選ぶ形にした（`components/PdfExportDialog.tsx`）。

- **用紙サイズ:** A4 / A3 / A2
- **向き:** 自動（図の縦横比で決定）/ 横 / 縦
- **収め方:** 1枚に収める / 実寸を保って分割する（75% / 100% / 150%）
- **結果の即時表示:** 設定を変えるたびに「3ページ（横3 × 縦1）・100%の大きさ」のように更新される。
  **出力してみるまで結果が分からないことが元の問題**だったため、ここが対応の中心にある
- **判読できない縮小の警告:** 1枚に収めて0.6倍を下回る場合に警告し、
  「A3に変更する」「分割して書き出す」を1クリックで適用できる

割り付け計算は `utils/pdfLayout.ts` に純関数として分離し、17件のテストで固定した。
分割時はラスタライズ済みのCanvasをページ単位で切り出し、各ページ隅に
「1-2 / 縦1×横3」の位置表示を入れる（貼り合わせられないと意味がないため）。

**残る改善余地:** 人物一覧を別ページとして添える案は未対応。Excel側で代替できている。

**Excel出力の確認** 本籍の列がない（G-01の波及）。それ以外は要件を満たす。

### 2.8 利用者と権限（要件5）

`lib/auth/permissions.ts` の各関数と要件の権限表は一致している。
アクセス制御は画面表示だけでなく DB の RLS で強制されており、要件8「案件へのアクセス」を満たす。

**G-09（解消済み）** 記録・画面・テーブルをすべて削除した。

- `app/settings/audit/`、`lib/db/audit.ts`、`AppHeader` のリンクを削除
- `lib/db/{projects,members,kosekiFiles}.ts` の `logAudit` 呼び出しを削除
- `supabase/migrations/0007_drop_audit_logs.sql` で `audit_logs` を削除。
  `create_organization` / `accept_pending_invitations` / `create_project` の3つの
  SECURITY DEFINER 関数がこのテーブルへ insert していたため、
  **テーブルを落とす前に関数を作り直している**（順序を逆にすると関数が壊れる）

記録済みのログは戸籍に関する個人情報を含みうるため、残さず削除する方針とした。

### 2.9 読み取り精度の施策（要件6）

| 施策 | 実装 | 現況 |
|---|---|---|
| モデルの比較検証と選定 | `scripts/benchmark/run.ts` | **仕組みのみ。未実施（G-07）** |
| 論理矛盾の自動検出 | `utils/consistency.ts` | 実装済み |
| 複数モデルによる照合 | `lib/analysis/ensemble.ts` | 実装済みだが**既定オフ（G-05）** |
| 読み取り指示の調整 | `lib/koseki-prompt.ts` | 継続対応 |
| 画像処理の検討 | — | **未着手（G-06）** |

要件6は「実施する事項」として契約に含めているため、**G-06 と G-07 は債務の未履行にあたる。**
検収項目にも「第6章に掲げる各施策が実装されていること」を含めているので、優先度を上げる必要がある。

**G-05: 2モデル照合が実質使われない（解消済み）**

`ANALYSIS_ENSEMBLE=true` を設定しないと動かず、`.env.example` にも記載がなかった。

**対応済み。APIキーが2社以上あれば既定で有効にした。** `ANALYSIS_ENSEMBLE=false` で明示的に止められる。
1社分しかキーがない場合は照合しようがないので静かに無効になる。

**あわせて同時実行に変更した。** 従来は primary の完了後に照合を走らせており、
解析時間が単純に2倍になっていた。この状態では既定オンにできない。
両者を同時に開始し、実際の待ち時間は「遅いほうの1回分」で済むようにした。

照合は先頭候補を前提に開始する。primaryがフォールバックして照合先と同じプロバイダに
なった場合は、比較しても意味がないため結果を破棄する（ログに残す）。

**G-06: 画像の前処理**

解像度の正規化、傾き補正、コントラスト調整のいずれも未実装で、アップロードされた原寸をそのまま送っている。
スマートフォン撮影の戸籍では精度への影響が大きいと見込まれる。

注意: 前処理は精度に直結するため、**コスト削減目的で先に入れてはならない。**
ベンチマークに条件として載せ、精度が落ちない範囲を実測してから決める。

**G-07: モデルの比較検証**

`pnpm benchmark` で3社を同一条件で流し、精度（適合率・再現率・F1）と実測原価を1つのレポートに出せる状態。
実データ（`testdata/`）と正解データの作成待ち。**正解データの作成が最も重い工程**だが、
一度作ればモデル変更・プロンプト変更のたびに再利用できる。

---

## 3. 設定と運用の穴

### G-10: `AI_NO_TRAINING_CONFIRMED` がコメントアウト（解消済み）

`.env.example` で `# AI_NO_TRAINING_CONFIRMED=true` とコメントアウトされている。
本番でこれが `true` でないと `checkDataPolicy()` が解析を止める（`lib/analysis/dataPolicy.ts`）。

意図した設計（未確認のまま機微情報を送らせない）だが、
**デプロイ直後に「解析が動かない」で詰まる導線**になっていた。

**対応済み。** `.env.example` で既定を有効にし、
「本番でこれが true でないと解析は必ずエラーになる」ことを警告として明記した。
仕組み自体（未確認なら解析を止める）は維持している。

### G-11: Geminiのモデル解決が未検証

公式ドキュメント上のエンドポイントは `gemini-3.1-pro-preview` だが、既定は `gemini-3.1-pro`。
素のIDが解決しない場合、フォールバックで `gemini-3.1-pro-preview` → `gemini-2.5-pro` と落ちる
（`lib/analysis/chain.ts`、対策済み）。

ただし**落ちたことに気づくにはログを見る必要がある。** APIキーを入れて1回動かせば
`logTokenUsage` とフォールバック警告で判明する。運用開始前に必ず確認すること。

### G-12: Geminiのプロンプトキャッシュ

固定プロンプトは約3,900トークンで、Gemini 3.x系の最小4,096トークンをわずかに下回る。
`responseSchema` を含めれば超える可能性もあるが未確認。
`logTokenUsage` がヒット率0%のとき警告を出すので、実測で判定する。

精度には影響しない（課金のみ）ため優先度は低い。

### G-13: `public/` の実データ（解消済み）

以下が残置されている。

- `public/abukike_2025-07-29T16-42-29-687Z.json`
- `public/family-info-sep.json`

`public/` 配下は**ビルドに含まれ、URLを知っていれば誰でも取得できる。**

**対応済み。** gitignore 済みの `testdata/` へ退避し、リポジトリの追跡からも外した
（`git rm --cached`）。手元のファイルは残しているのでベンチマークにそのまま使える。

**注意: 過去のコミット履歴にはファイルが残っている。** 外部へ公開する段階では
履歴の書き換え（filter-repo 等）が必要になる。現時点では非公開リポジトリのため未対応。

---

## 3.5 テスト

| 層 | 実行 | 対象 |
|---|---|---|
| 単体 | `pnpm test`（Vitest, 126件） | 純関数。レイアウト計算・矛盾検出・2モデル照合・名寄せ・数え年・プロバイダチェーン |
| E2E | `pnpm e2e`（Playwright, 21件） | ブラウザ上の実挙動。描画・選択・指摘一覧・要確認の印・ズーム・全体表示・戸籍（本籍）・PDF書き出し設定 |

**E2Eの構成:** `app/e2e-fixture/` に固定データの画面を置き、そこに対してテストする。

**本番ビルドに対して実行する。** devサーバーはルートを遅延コンパイルするため、
並列実行するとハイドレーションが数秒〜十数秒遅れ、テストが不安定になる
（実際に5/21が落ちる状態だった）。本番ビルドなら待ち時間が一定で、
利用者が触るものとも一致する。

フィクスチャ画面は `data-hydrated` 属性でハイドレーション完了を通知する。
カードはSSRの時点で描画されるため、カードの表示だけを待つと
クリックが効かない状態で操作してしまう。

家系図エディタの中身はDBに依存しないため、認証とSupabaseを介さずに検証できる。
これがないとE2Eはログイン画面より先に進めず、価値のある部分を一切触れない。

**本番では二重にガードしている。**
- 画面側: `E2E_FIXTURES !== '1'` なら `notFound()`
- ミドルウェア側: 同じ環境変数が立っていなければ認証必須のパスとして扱う

**この層でしか守れないもの:** G-02 は「検出も保存もされているが画面に出ない」という不具合だった。
純関数のテストは全て通っていたため、単体テストでは永久に検出できない。
同種の不具合を防ぐため、指摘一覧のテストを重点的に置いている。

固定データには、実際の解析で起きうる状況を意図的に含めている
（元号の読み違いによる矛盾、西暦に変換できない日付、人物に紐づかない照合の指摘）。

---

## 4. 対応順序の提案

**第1段階（実務で使えるようにする）** — **完了**
1. ~~G-02 + G-03 指摘一覧の実装~~
2. ~~G-01 本籍の抽出~~（戸籍を独立要素として追加）
3. ~~G-08 PDFの複数ページ対応~~

**第2段階（契約上の債務）**
4. G-07 ベンチマークの実施 — 実データの用意が前提
5. G-06 画像前処理 — ベンチマークの結果を見てから
6. G-05 照合の既定オン化と `.env.example` 追記

**第3段階（整合性）**
7. ~~G-04 Googleログインの削除~~ — **完了**（Supabase側のプロバイダ無効化のみ残る）
8. ~~G-09 監査ログの扱いを決定~~ — **完了**（全削除）
9. G-13 実データの退避
10. G-14〜G-17 要件定義書の追補

**運用開始前の確認**
- G-10 `AI_NO_TRAINING_CONFIRMED` の設定
- G-11 Geminiのモデル解決をログで確認
- G-12 キャッシュヒット率をログで確認

---

## 5. 本書の保守

要件定義書を改訂した場合、および実装を変更した場合は本書も更新する。
乖離が解消したら該当行を「解消済み」として残し、削除しない（判断の経緯が失われるため）。
