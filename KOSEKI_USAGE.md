# 戸籍書類解析機能 使用方法

戸籍謄本・除籍謄本・改製原戸籍のPDFまたは画像（JPEG/PNG/WebP）をアップロードすると、
Gemini AIが内容を解析して家系図データを自動抽出します。

## 使用手順

1. 案件を開き、左サイドバーの「戸籍PDFをアップロード」をクリック
2. ファイルを選択（複数選択・ドラッグ＆ドロップ可。各20MBまで）
3. 「解析」ボタンをクリックすると、順にアップロード → 解析 → 家系図へ取り込みが行われます
4. 取り込み後は自動でサーバーに保存されます

## 複数書類の名寄せ（重複人物の統合）

戸籍は改製・転籍により同一人物が複数の書類に登場します。
解析結果のマージ時に、以下のルールで同一人物を自動判定して1人に統合します:

1. IDが一致する
2. 氏名が一致し、生年が一致する
3. 氏名が一致し、双方の生年が不明で没年が一致する
4. 氏名が一致し、片方の生年のみ不明で、その氏名の人物が既存に1人しかいない

同姓同名でも生年が異なる場合（襲名・親子の同名など）は別人として扱われます。
誤って統合された/されなかった場合は、人物の編集・削除で手動修正してください。

## 保存されるファイル

アップロードされた書類は案件に紐づけて非公開ストレージに保存され、
左サイドバーの「戸籍ファイル」一覧から再解析・ダウンロード・削除ができます。

## 注意事項

1. **精度**: AIによる解析のため、結果を必ず確認し、必要に応じて手動で修正してください
2. **画質**: 手書き・低画質の書類は精度が下がります。可能な限り高解像度のスキャン・撮影を使用してください
3. **プライバシー**: 書類は解析のためGoogle Gemini APIに送信されます。実運用では
   データが学習に使用されない有料枠の利用を推奨します
4. **レート制限**: 解析は1ユーザーあたり10分間に20回までに制限されています

## 抽出データの構造

抽出されるJSONは `people`（人物）と `families`（家族関係）の2配列で構成されます。
詳細な構造は `utils/familyDataProcessor.ts` の型定義を参照してください。
